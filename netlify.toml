[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "18"

# Next.js rewrites and redirects are handled by Next.js itself
# Netlify will serve the built Next.js app


